{% extends 'base.html.twig' %}

{% block title %}Chambre{% endblock %}

{% block body %}
    <h1>Chambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Numero</th>
                <td>{{ chambre.numero }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ chambre.prix }}</td>
            </tr>
            <tr>
                <th>NbrLit</th>
                <td>{{ chambre.nbrLit }}</td>
            </tr>
            <tr>
                <th>NbrPers</th>
                <td>{{ chambre.nbrPers }}</td>
            </tr>
            <tr>
                <th>Confort</th>
                <td>{{ chambre.confort }}</td>
            </tr>
            <tr>
                <th>Dateariv</th>
                <td>{{ chambre.dateariv }}</td>
            </tr>
            <tr>
                <th>Datedep</th>
                <td>{{ chambre.datedep }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('chambre_index') }}">back to list</a>

    <a href="{{ path('chambre_edit', {'numero': chambre.numero}) }}">edit</a>

    {{ include('chambre/_delete_form.html.twig') }}
{% endblock %}

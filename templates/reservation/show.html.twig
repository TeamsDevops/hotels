{% extends 'base.html.twig' %}

{% block title %}Reservation{% endblock %}

{% block body %}
    <h1>Reservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdRes</th>
                <td>{{ reservation.idRes }}</td>
            </tr>
            <tr>
                <th>DateAriv</th>
                <td>{{ reservation.dateAriv }}</td>
            </tr>
            <tr>
                <th>DateDep</th>
                <td>{{ reservation.dateDep }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_index') }}">back to list</a>

    <a href="{{ path('reservation_edit', {'idRes': reservation.idRes}) }}">edit</a>

    {{ include('reservation/_delete_form.html.twig') }}
{% endblock %}
